{
  "name": "@jeff-the-chef/backend",
  "version": "0.1.0",
  "private": true,
  "description": "Provider-neutral API gateway for Jeff The Chef.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsx watch src/index.ts",
    "start": "bun dist/index.js",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "fastify": "^5.8.5",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "tsx": "^4.22.4",
    "vitest": "^4.1.9"
  }
}
